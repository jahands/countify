{
	"name": "permissions",
	"private": true,
	"version": "0.1.0",
	"description": "Manage permissions for the app",
	"main": "src/index.ts",
	"scripts": {
		"build": "tsc",
		"test": "vitest run",
		"test:watch": "vitest"
	},
	"author": "",
	"license": "ISC",
	"dependencies": {
		"permissible": "*"
	},
	"devDependencies": {
		"vitest": "^0.32.0",
		"tsconfig": "*",
		"eslint-config-custom": "*"
	}
}
